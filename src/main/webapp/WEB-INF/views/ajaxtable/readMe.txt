3. Заполнить динамически таблицу. [#58533]
 Администратор,  25.06.18 3:34
Задание.

1. В задании  1. index.html + bootstrap, form, table [#58522] добавьте поведение, которые добавляет новую строку с заполенными полями в таблицу.

Допустим есть таблица с id='table'

<table id='table'>
  <tbody>
    <tr>...</tr>
    <tr>...</tr>
  </tbody>
</table>

Добавим кнопку с повесим на ее фукнцию onclick="addRow()"
вызов JS

function addRow() {

      //получаем значение поля usr

    var name = $('#usr').val();

    //получаем ссылку на последний элемент в таблице.
    //и после него добавляем html
    $('#table tr:last').after('<tr><td>' + name +'</td></tr>');
}
